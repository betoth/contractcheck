/**
 * Barrel file for shared utilities.
 *
 * - Re-exports all functions from `lib/utils`.
 * - Keeps imports clean and consistent across the app.
 * - Acts as a central entrypoint for utility helpers.
 *
 * Usage:
 *   import { cn, formatDate } from "@/shared/utils"
 */

export * from "@/shared/lib/utils"
