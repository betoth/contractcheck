/**
 * Barrel file for AppSidebar.
 *
 * - Re-exports AppSidebar component from its implementation file.
 * - Keeps import paths clean and consistent across the app.
 *
 * Usage:
 *   import { AppSidebar } from "@/shared/components/organisms/AppSidebar"
 */
export { default as AppSidebar } from "./AppSidebar.jsx"
