/**
 * Barrel file for AppHeader
 *
 * Exports the AppHeader component as default.
 * This ensures cleaner imports across the app:
 *
 *   import { AppHeader } from "@/shared/components/organisms/AppHeader"
 */
export { default as AppHeader } from "./AppHeader.jsx"
